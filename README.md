# Jobpostr Adonis fullstack application

## Setup
1. clone the project & create new database & rename .env.example to .env than add database name / host / password
2. `npm install`
3. `adonis serve --dev`

